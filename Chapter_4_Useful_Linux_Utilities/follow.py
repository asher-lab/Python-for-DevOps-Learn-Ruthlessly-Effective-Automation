import subprocess

subprocess.call(['ls','-alh'])

# run a subprocess,like in bash ls -alh

#usage = strace python3 follow.py
# strace is a process of tracing the path of the program starting from its CLI call up until the command is finished
